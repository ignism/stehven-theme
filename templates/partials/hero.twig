<div class="w-full h-full flex flex-col md:justify-between px-6 md:px-8 lg:px-12 py-12 bg-{{ background }}">
    <div class="w-full">
        <h2>{{ title }}</h2>
        <p class="text-xl md:text-3xl mb-8">
            {{ subtitle }}
        </p>
    </div>

    <div class="w-full">
        <p class="md:w-3/4 text-base font-light md:text-2xl">
            {{ paragraph }}
        </p>
    </div>
</div>